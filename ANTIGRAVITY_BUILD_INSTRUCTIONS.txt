================================================================================
NUVANA — FULL-STACK BUILD BRIEF FOR GOOGLE ANTIGRAVITY IDE
Turn the "Nuvana" dry-fruits design prototype into a live, deployed shop.
Stack: Frontend + Backend on Vercel, Database + Auth on Supabase (via MCP).
================================================================================

HOW TO USE THIS FILE
--------------------
1. Open the existing prototype folder in Antigravity IDE:
      C:\Users\misur\Desktop\Ecom Dry Fruit
   The prototype is the SOURCE OF TRUTH for design, copy, catalog and sample
   data. The canonical data lives in  assets/data.js  (object window.NUVANA).
2. Make sure these MCP servers are connected in Antigravity BEFORE you start:
      - Supabase MCP   (create project, run SQL migrations, seed, manage auth)
      - Vercel MCP     (create/link project, set env vars, deploy, read logs)
   If either is missing, stop and ask the user to connect it.
3. Paste this entire file to the agent as the task brief. Work top to bottom.
   Do NOT skip the RLS, triggers, or the VERIFICATION CHECKLIST at the end.
4. When done, hand back: the live Vercel URL, the Supabase project ref, the
   admin login, and a filled-in copy of the VERIFICATION CHECKLIST.

DECISIONS ALREADY MADE BY THE OWNER (do not re-ask):
   - Framework: YOUR CHOICE. Pick the best architecture for a Vercel + Supabase
     shop. Recommended default: Next.js (App Router, TypeScript, Server
     Components + Route Handlers/Server Actions) — it is the cleanest fit for
     Vercel + Supabase and for everything below. If you choose otherwise, it
     MUST support server-side rendering / serverless functions on Vercel, the
     Supabase JS client, and Supabase Auth with row-level security.
   - Auth: FULL. Customers can sign up / log in; the admin panel is gated behind
     an "admin" role.
   - Scope: FULL WORKING SHOP. Live catalog, cart, checkout that writes real
     orders and decrements stock, customer accounts, review submission +
     moderation, and an admin dashboard whose analytics come from live data.

GROUND RULES
------------
- Preserve the Nuvana look exactly: reuse assets/styles.css (the design system,
  colour tokens, fonts) and port the existing components/markup from
  assets/*.js. Do not restyle into a generic template. Keep the festive
  graphics (assets/graphics.js motifs, product illustrations in assets/icons.js)
  — port them as SVG React components.
- Currency is INR (₹) throughout. Keep the Indian number formatting from
  data.js (fmtINR / fmtK).
- Keep product illustrations as the default media, and keep the "photo slot":
  a product's image_url, when present, overlays the illustration; if it is empty
  or fails to load, the illustration shows. (Mirror the ProductMedia behaviour
  in assets/ui.js.)
- All prices, stock and money math happen on the server. Never trust a cart
  total sent from the browser — recompute it from the database at checkout.
- Everything must be idempotent and re-runnable: use "create ... if not exists",
  "on conflict do nothing/update", and versioned migrations so re-running does
  not duplicate data.


================================================================================
SECTION 1 — ENVIRONMENT & SECRETS
================================================================================
Create a Supabase project (region closest to India, e.g. ap-south-1 / Mumbai).
Create a Vercel project linked to the app's git repo.

Set these environment variables in BOTH local (.env.local) and Vercel:
   NEXT_PUBLIC_SUPABASE_URL          = https://<project-ref>.supabase.co
   NEXT_PUBLIC_SUPABASE_ANON_KEY     = <anon public key>
   SUPABASE_SERVICE_ROLE_KEY         = <service role key>   (server-side ONLY,
                                        never exposed to the browser)
   NEXT_PUBLIC_SITE_URL              = <the Vercel production URL>
(If you pick a non-Next framework, use the equivalent public/secret split — the
anon key is browser-safe; the service-role key is server-only.)

Enable Supabase Storage. Create a PUBLIC bucket named  product-images  for
product photos (admin uploads write here; the public reads).


================================================================================
SECTION 2 — DATABASE SCHEMA (Supabase / PostgreSQL)
================================================================================
Apply the following as SQL migrations through the Supabase MCP. Order matters:
extensions -> enums -> tables -> indexes -> triggers/functions -> RLS -> views.

Naming: snake_case tables and columns. Money is stored as INTEGER in paise-free
whole rupees (the prototype uses whole-rupee INR), column suffix _inr. Timestamps
are timestamptz default now().

--- 2.1 Extensions -----------------------------------------------------------
```sql
create extension if not exists "pgcrypto";   -- gen_random_uuid()
```

--- 2.2 Enums ----------------------------------------------------------------
```sql
do $$ begin
  create type user_role       as enum ('customer','staff','admin');
  create type customer_tier   as enum ('Silver','Gold','Platinum');
  create type order_status    as enum ('pending','processing','shipped','delivered','cancelled');
  create type payment_method  as enum ('UPI','COD','Card','Net Banking');
  create type order_channel   as enum ('Web','App');
  create type review_status   as enum ('pending','published','hidden');
  create type product_flag    as enum ('best','sale','new','low','out');
exception when duplicate_object then null; end $$;
```

--- 2.3 profiles  (extends Supabase auth.users) ------------------------------
One row per authenticated user. id equals auth.users.id.
```sql
create table if not exists profiles (
  id           uuid primary key references auth.users(id) on delete cascade,
  full_name    text not null default '',
  email        text unique,
  phone        text,
  role         user_role not null default 'customer',
  city         text,
  tier         customer_tier not null default 'Silver',
  member_since date not null default current_date,
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
```

--- 2.4 categories -----------------------------------------------------------
id is the slug used in the prototype (matches product.cat).
```sql
create table if not exists categories (
  id           text primary key,            -- 'almonds','cashews',...
  name         text not null,
  glyph        text not null,               -- illustration key in icons.js
  tint         text not null,               -- hex swatch, e.g. '#F3E7CE'
  display_count int not null default 0,     -- marketing count shown on the tile
  sort_order   int not null default 0,
  created_at   timestamptz not null default now()
);
```

--- 2.5 products -------------------------------------------------------------
id keeps the prototype code ('p01'..'p14') as a stable natural key.
grad_from/grad_to are the two media-background hex stops.
rating_avg and reviews_count are DENORMALISED (kept in sync by a trigger from
the reviews table — see 2.12). sold_count increments at checkout.
```sql
create table if not exists products (
  id           text primary key,            -- 'p01'...
  category_id  text not null references categories(id),
  name         text not null,
  glyph        text not null,
  grad_from    text not null,
  grad_to      text not null,
  image_url    text default '',             -- photo slot; empty => show glyph
  price_inr    int  not null check (price_inr >= 0),
  mrp_inr      int  not null default 0,      -- 0 => no strike-through
  unit         text not null,               -- '250 g','500 g','1 g','box'
  origin       text,
  blurb        text,
  stock        int  not null default 0 check (stock >= 0),
  sold_count   int  not null default 0,
  flags        product_flag[] not null default '{}',
  rating_avg   numeric(2,1) not null default 0,
  reviews_count int not null default 0,
  is_active    boolean not null default true,
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists idx_products_category on products(category_id);
create index if not exists idx_products_active   on products(is_active);
```

--- 2.6 addresses ------------------------------------------------------------
```sql
create table if not exists addresses (
  id         uuid primary key default gen_random_uuid(),
  user_id    uuid not null references profiles(id) on delete cascade,
  line1      text not null,
  line2      text,
  city       text not null,
  state      text,
  pincode    text not null,
  phone      text,
  is_default boolean not null default false,
  created_at timestamptz not null default now()
);
create index if not exists idx_addresses_user on addresses(user_id);
```

--- 2.7 carts / cart_items  (server-persisted cart; optional but recommended) -
A cart may belong to a logged-in user or an anonymous session token.
```sql
create table if not exists carts (
  id            uuid primary key default gen_random_uuid(),
  user_id       uuid references profiles(id) on delete cascade,
  session_token text,
  created_at    timestamptz not null default now(),
  updated_at    timestamptz not null default now()
);
create table if not exists cart_items (
  id         uuid primary key default gen_random_uuid(),
  cart_id    uuid not null references carts(id) on delete cascade,
  product_id text not null references products(id),
  unit       text not null,
  qty        int  not null check (qty > 0),
  unique (cart_id, product_id, unit)
);
```

--- 2.8 orders --------------------------------------------------------------
order_no is the human code ('NX-24817'). user_id is null for guest orders.
customer_name/city are snapshots so the order reads correctly even if the
profile changes later. Money is recomputed server-side at checkout.
```sql
create table if not exists orders (
  id             uuid primary key default gen_random_uuid(),
  order_no       text unique not null,
  user_id        uuid references profiles(id) on delete set null,
  customer_name  text not null,
  city           text,
  status         order_status   not null default 'pending',
  payment_method payment_method not null default 'UPI',
  channel        order_channel  not null default 'Web',
  items_count    int  not null default 0,
  subtotal_inr   int  not null default 0,
  shipping_inr   int  not null default 0,   -- free over ₹999, else ₹49
  total_inr      int  not null default 0,
  placed_at      timestamptz not null default now(),
  created_at     timestamptz not null default now(),
  updated_at     timestamptz not null default now()
);
create index if not exists idx_orders_user   on orders(user_id);
create index if not exists idx_orders_status on orders(status);
create index if not exists idx_orders_placed on orders(placed_at);
```

--- 2.9 order_items ---------------------------------------------------------
Line items with a price snapshot (unit_price_inr) taken at purchase time.
```sql
create table if not exists order_items (
  id             uuid primary key default gen_random_uuid(),
  order_id       uuid not null references orders(id) on delete cascade,
  product_id     text not null references products(id),
  product_name   text not null,             -- snapshot
  unit           text not null,
  unit_price_inr int  not null,
  qty            int  not null check (qty > 0),
  line_total_inr int  not null
);
create index if not exists idx_order_items_order   on order_items(order_id);
create index if not exists idx_order_items_product on order_items(product_id);
```

--- 2.10 reviews ------------------------------------------------------------
```sql
create table if not exists reviews (
  id          uuid primary key default gen_random_uuid(),
  product_id  text not null references products(id) on delete cascade,
  user_id     uuid references profiles(id) on delete set null,
  author_name text not null,
  rating      int  not null check (rating between 1 and 5),
  body        text not null,
  status      review_status not null default 'pending',
  created_at  timestamptz not null default now()
);
create index if not exists idx_reviews_product on reviews(product_id);
create index if not exists idx_reviews_status  on reviews(status);
```

--- 2.11 testimonials & store_settings --------------------------------------
```sql
create table if not exists testimonials (
  id          uuid primary key default gen_random_uuid(),
  quote       text not null,
  author      text not null,
  is_featured boolean not null default true,
  sort_order  int not null default 0
);

create table if not exists store_settings (
  id       int primary key default 1 check (id = 1),  -- single row
  name     text not null,
  tagline  text not null,
  est      text,
  phone    text,
  free_shipping_threshold_inr int not null default 999,
  shipping_flat_inr           int not null default 49,
  updated_at timestamptz not null default now()
);
```

--- 2.12 Triggers & functions -----------------------------------------------
(a) keep updated_at fresh:
```sql
create or replace function set_updated_at() returns trigger language plpgsql as $$
begin new.updated_at = now(); return new; end $$;

do $$ begin
  create trigger trg_profiles_updated before update on profiles
    for each row execute function set_updated_at();
  create trigger trg_products_updated before update on products
    for each row execute function set_updated_at();
  create trigger trg_orders_updated   before update on orders
    for each row execute function set_updated_at();
exception when duplicate_object then null; end $$;
```
(b) roll up product rating_avg / reviews_count from PUBLISHED reviews:
```sql
create or replace function refresh_product_rating() returns trigger
language plpgsql as $$
declare pid text := coalesce(new.product_id, old.product_id);
begin
  update products p set
    rating_avg = coalesce((select round(avg(rating)::numeric,1)
                           from reviews r
                           where r.product_id = pid and r.status='published'),0),
    reviews_count = (select count(*) from reviews r
                     where r.product_id = pid and r.status='published')
  where p.id = pid;
  return null;
end $$;

do $$ begin
  create trigger trg_reviews_rollup
    after insert or update or delete on reviews
    for each row execute function refresh_product_rating();
exception when duplicate_object then null; end $$;
```
(c) auto-create a profile when a new auth user signs up:
```sql
create or replace function handle_new_user() returns trigger
language plpgsql security definer set search_path = public as $$
begin
  insert into profiles (id, email, full_name)
  values (new.id, new.email,
          coalesce(new.raw_user_meta_data->>'full_name',''))
  on conflict (id) do nothing;
  return new;
end $$;

do $$ begin
  create trigger on_auth_user_created
    after insert on auth.users
    for each row execute function handle_new_user();
exception when duplicate_object then null; end $$;
```

--- 2.13 place_order()  — transactional checkout RPC ------------------------
Call this from the server at checkout. It validates stock, computes money from
DB prices, writes the order + items, decrements stock, bumps sold_count, and
returns the new order_no. SECURITY DEFINER so it can update stock under RLS.
First create the order-number sequence it depends on (starts ABOVE the sample
data so live checkouts never collide with the 12 seeded orders):
```sql
create sequence if not exists order_no_seq start with 24818;
```
```sql
create or replace function place_order(
  p_user_id  uuid,
  p_name     text,
  p_city     text,
  p_payment  payment_method,
  p_channel  order_channel,
  p_items    jsonb              -- [{"product_id":"p01","unit":"250 g","qty":2}, ...]
) returns text
language plpgsql security definer set search_path = public as $$
declare
  v_order_id uuid; v_no text; v_sub int := 0; v_ship int := 0; v_cnt int := 0;
  it jsonb; v_prod products%rowtype; v_line int;
begin
  if p_items is null or jsonb_array_length(p_items) = 0 then
     raise exception 'Cart is empty';
  end if;
  v_no := 'NX-' || to_char(nextval('order_no_seq'), 'FM00000');
  insert into orders(order_no,user_id,customer_name,city,status,payment_method,channel)
    values (v_no,p_user_id,p_name,p_city,'processing',p_payment,p_channel)
    returning id into v_order_id;

  for it in select * from jsonb_array_elements(p_items) loop
    select * into v_prod from products
      where id = (it->>'product_id') for update;
    if not found then raise exception 'Unknown product %', it->>'product_id'; end if;
    if v_prod.stock < (it->>'qty')::int then
      raise exception 'Insufficient stock for %', v_prod.name; end if;

    v_line := v_prod.price_inr * (it->>'qty')::int;
    v_sub  := v_sub + v_line;
    v_cnt  := v_cnt + (it->>'qty')::int;

    insert into order_items(order_id,product_id,product_name,unit,unit_price_inr,qty,line_total_inr)
      values (v_order_id, v_prod.id, v_prod.name, coalesce(it->>'unit',v_prod.unit),
              v_prod.price_inr, (it->>'qty')::int, v_line);

    update products set stock = stock - (it->>'qty')::int,
                        sold_count = sold_count + (it->>'qty')::int
      where id = v_prod.id;
  end loop;

  v_ship := case when v_sub >= 999 then 0 else 49 end;
  update orders set items_count=v_cnt, subtotal_inr=v_sub,
                    shipping_inr=v_ship, total_inr=v_sub+v_ship
    where id = v_order_id;
  return v_no;
end $$;
```

--- 2.14 Analytics VIEWS  (dashboard reads these — always live) --------------
```sql
-- Revenue / orders / AOV for a rolling window (dashboard KPI cards)
create or replace view v_kpis as
select
  coalesce(sum(total_inr),0)                              as revenue_inr,
  count(*)                                                as orders_count,
  coalesce(round(avg(total_inr)),0)                       as aov_inr
from orders where status <> 'cancelled';

create or replace view v_new_customers as
select count(*) as new_customers
from profiles where role='customer'
  and created_at >= date_trunc('month', now());

create or replace view v_orders_by_month as
select to_char(date_trunc('month', placed_at),'Mon') as m,
       date_trunc('month', placed_at)                as month_start,
       count(*)                                      as v
from orders where status <> 'cancelled'
group by 1,2 order by 2;

create or replace view v_category_split as
select c.name,
       round(100.0 * sum(oi.line_total_inr) /
             nullif(sum(sum(oi.line_total_inr)) over (),0), 0) as value
from order_items oi
join products p   on p.id = oi.product_id
join categories c on c.id = p.category_id
group by c.name order by value desc;

create or replace view v_top_products as
select p.id, p.name, sum(oi.qty) as units, sum(oi.line_total_inr) as revenue
from order_items oi join products p on p.id = oi.product_id
group by p.id, p.name order by units desc limit 5;
```


================================================================================
SECTION 3 — ROW LEVEL SECURITY (turn on for EVERY table)
================================================================================
Enable RLS on all tables, then add policies. Helper for admin checks:
```sql
create or replace function is_admin() returns boolean
language sql stable security definer set search_path=public as $$
  select exists(select 1 from profiles where id = auth.uid()
                and role in ('admin','staff'));
$$;
```
Policies:
```sql
-- profiles
alter table profiles enable row level security;
create policy "profiles self read"   on profiles for select using (id = auth.uid() or is_admin());
create policy "profiles self update" on profiles for update using (id = auth.uid());
create policy "profiles admin all"   on profiles for all    using (is_admin()) with check (is_admin());

-- categories & products: public can read active catalog; admins write
alter table categories enable row level security;
create policy "cat public read" on categories for select using (true);
create policy "cat admin write" on categories for all using (is_admin()) with check (is_admin());

alter table products enable row level security;
create policy "prod public read" on products for select using (is_active or is_admin());
create policy "prod admin write" on products for all using (is_admin()) with check (is_admin());

-- addresses / carts / cart_items: owner only
alter table addresses enable row level security;
create policy "addr owner" on addresses for all using (user_id = auth.uid()) with check (user_id = auth.uid());

alter table carts enable row level security;
create policy "cart owner" on carts for all using (user_id = auth.uid()) with check (user_id = auth.uid());
alter table cart_items enable row level security;
create policy "cart_items owner" on cart_items for all
  using (exists(select 1 from carts c where c.id=cart_id and c.user_id=auth.uid()))
  with check (exists(select 1 from carts c where c.id=cart_id and c.user_id=auth.uid()));

-- orders / order_items: customer sees own, admin sees all. Inserts go through
-- place_order() (SECURITY DEFINER), so no broad insert policy is needed.
alter table orders enable row level security;
create policy "orders own read"  on orders for select using (user_id = auth.uid() or is_admin());
create policy "orders admin write" on orders for update using (is_admin()) with check (is_admin());
alter table order_items enable row level security;
create policy "order_items read" on order_items for select
  using (exists(select 1 from orders o where o.id=order_id
               and (o.user_id=auth.uid() or is_admin())));

-- reviews: anyone reads PUBLISHED; logged-in users insert (as pending);
-- admins moderate.
alter table reviews enable row level security;
create policy "rev public read" on reviews for select using (status='published' or is_admin());
create policy "rev insert" on reviews for insert with check (auth.uid() is not null);
create policy "rev admin update" on reviews for update using (is_admin()) with check (is_admin());

-- testimonials & settings: public read, admin write
alter table testimonials enable row level security;
create policy "tst read" on testimonials for select using (true);
create policy "tst admin" on testimonials for all using (is_admin()) with check (is_admin());
alter table store_settings enable row level security;
create policy "set read" on store_settings for select using (true);
create policy "set admin" on store_settings for all using (is_admin()) with check (is_admin());
```
Grant execute on the RPCs to authenticated + anon (guest checkout allowed):
```sql
grant execute on function place_order(uuid,text,text,payment_method,order_channel,jsonb) to anon, authenticated;
```


================================================================================
SECTION 4 — SEED DATA  (every entry from assets/data.js)
================================================================================
Read assets/data.js as the canonical source and load ALL of it. The complete
values are reproduced below so this file is self-contained. Use idempotent
upserts (on conflict do update/nothing).

--- 4.1 store_settings (1 row) ----------------------------------------------
name='Nuvana', tagline='Premium Dry Fruits & Nuts', est='EST · 2019',
phone='+91 98200 41000', free_shipping_threshold_inr=999, shipping_flat_inr=49.

--- 4.2 categories (6) --------------------------------------------------------
id | name | glyph | tint | display_count | sort_order
almonds    | Almonds      | almond    | #F3E7CE | 14 | 1
cashews    | Cashews      | cashew    | #EEE6D2 | 11 | 2
pistachios | Pistachios   | pistachio | #E4EBD3 |  9 | 3
walnuts    | Walnuts      | walnut    | #EADFCE |  8 | 4
dried      | Dried Fruits | fig       | #EEDDE0 | 17 | 5
gifting    | Gift Boxes   | gift      | #F6E6C7 |  6 | 6

--- 4.3 products (14) — grad_from/grad_to per category glyph -----------------
Gradient stops by glyph:
  almond #F6EAD0/#E7D3A9 · cashew #F3ECDC/#E4D6BB · pistachio #E7EFD6/#CDDCAF
  walnut #EEE0CB/#D8BE98 · date #EAD9C6/#CFA883 · raisin #E9E1CE/#CBB98F
  fig #EFDDE4/#D6A9BC · apricot #FBE7C7/#F2C583 · saffron #F7E3C4/#EAB05A
  gift #F3E6C9/#E4C88E

id  | name                     | category | glyph     | price | mrp  | unit  | stock | sold | flags        | origin              | rating | reviews
p01 | California Almonds       | almonds  | almond    | 399   | 460  | 250 g | 340   | 1820 | best         | California, USA     | 4.8 | 214
p02 | Kashmiri Mamra Almonds   | almonds  | almond    | 549   | 0    | 250 g | 120   | 430  | new          | Kashmir, India      | 4.9 | 96
p03 | Roasted Salted Cashews   | cashews  | cashew    | 449   | 520  | 250 g | 280   | 2140 | best, sale   | Mangalore, India    | 4.7 | 308
p04 | Jumbo Cashews W240       | cashews  | cashew    | 599   | 0    | 250 g | 64    | 520  | (none)       | Goa, India          | 4.6 | 74
p05 | Iranian Pistachios       | pistachios | pistachio | 649 | 720  | 250 g | 190   | 1360 | best         | Kerman, Iran        | 4.8 | 181
p06 | Salted Pistachios        | pistachios | pistachio | 559 | 0    | 250 g | 8     | 900  | low          | Kerman, Iran        | 4.5 | 120
p07 | Kashmiri Walnut Kernels  | walnuts  | walnut    | 699   | 0    | 250 g | 150   | 610  | new          | Kashmir, India      | 4.7 | 88
p08 | Inshell Walnuts          | walnuts  | walnut    | 449   | 0    | 500 g | 210   | 340  | (none)       | Chile               | 4.4 | 52
p09 | Medjool Dates            | dried    | date      | 499   | 560  | 250 g | 170   | 1980 | best, sale   | Jordan Valley       | 4.9 | 265
p10 | Afghan Green Raisins     | dried    | raisin    | 249   | 0    | 250 g | 0     | 1120 | out          | Kandahar, Afghanistan | 4.5 | 143
p11 | Turkish Dried Figs       | dried    | fig       | 399   | 0    | 200 g | 130   | 520  | (none)       | Aydin, Turkey       | 4.6 | 97
p12 | Dried Apricots           | dried    | apricot   | 329   | 0    | 250 g | 95    | 410  | (none)       | Hunza Valley        | 4.4 | 69
p13 | Pure Mongra Saffron      | gifting  | saffron   | 449   | 0    | 1 g   | 75    | 1450 | best, new    | Pampore, Kashmir    | 4.9 | 210
p14 | Signature Gift Hamper    | gifting  | gift      | 1299  | 1499 | box   | 40    | 760  | best, sale   | Assembled in India  | 4.8 | 132

Blurbs (blurb column, verbatim from data.js):
p01 Plump, sweet and crunchy — our everyday hero, sorted by hand and freshly packed.
p02 Rare, oil-rich mamra almonds prized in Ayurveda — denser, richer, more filling.
p03 Slow-roasted in small batches with a whisper of sea salt. Dangerously snackable.
p04 Grade W240 whole kernels — big, creamy and unbroken. The gifting-grade cashew.
p05 Long Akbari pistachios, roasted and lightly salted. Vivid green, buttery finish.
p06 The classic table pistachio — easy-open shells, perfectly salted.
p07 Light-halves walnut kernels — no shelling, no bitterness. Brain food, upgraded.
p08 Fresh-cracked at home — that first snap is half the pleasure.
p09 Soft, caramel-toffee Medjools — nature's dessert, no sugar added.
p10 Long, seedless and tart-sweet. The ones that vanish from the bowl first.
p11 Sun-dried Anjeer, soft and jammy with a honeyed seed crunch.
p12 Tangy, chewy Hunza apricots — sun-dried, unsulphured, honest.
p13 All-red Mongra threads, grade A1 — a pinch colours a whole pot of kheer.
p14 Six of our finest, boxed with a handwritten note. The gift people remember.
NOTE: set rating_avg/reviews_count to the values above as INITIAL seed; after
seeding the reviews (4.6) the trigger will recompute them from published reviews,
which is expected and fine.
NOTE: seed flags exactly as listed. p06 'low' and p10 'out' are stock signals —
keep them, and note p10 stock=0 (out of stock).

--- 4.4 auth users + customer profiles (8 customers + 1 admin) ---------------
Create these via Supabase Auth (admin create user / MCP), email confirmed=true,
then upsert the matching profiles row (the signup trigger creates the profile;
update role/city/tier/member_since/full_name to the values below).
Use a default password for all seed accounts:  Nuvana@123   (tell the owner to
rotate these after first login).

ADMIN:
  email admin@nuvana.shop | full_name 'Nuvana Admin' | role admin

CUSTOMERS (map to prototype c1..c8; tier + member_since + city as given):
key | full_name       | email                       | city       | tier     | since
c1  | Aarav Sharma    | aarav.sharma@gmail.com      | Mumbai     | Gold     | 2024-03-01
c2  | Diya Nair       | diya.nair@outlook.com       | Kochi      | Silver   | 2025-01-01
c3  | Kabir Mehta     | kabir.mehta@gmail.com       | Ahmedabad  | Platinum | 2023-08-01
c4  | Ananya Rao      | ananya.rao@gmail.com        | Bengaluru  | Gold     | 2024-11-01
c5  | Vivaan Gupta    | vivaan.g@yahoo.com          | Delhi      | Silver   | 2025-04-01
c6  | Isha Kulkarni   | isha.k@gmail.com            | Pune       | Gold     | 2024-06-01
c7  | Rohan Iyer      | rohan.iyer@gmail.com        | Chennai    | Silver   | 2025-07-01
c8  | Meera Joshi     | meera.joshi@gmail.com       | Jaipur     | Platinum | 2023-02-01
(The prototype also shows lifetime "orders" and "spent" per customer. Do NOT
store these as raw columns to hand-edit — they should read from live orders via
a view/aggregate. For display parity you MAY seed extra historical orders per
4.5 so the totals land near: c1 14/₹18,640 · c2 6/₹7,420 · c3 22/₹31,280 ·
c4 9/₹11,360 · c5 4/₹4,980 · c6 17/₹22,140 · c7 3/₹5,490 · c8 28/₹38,920.)

--- 4.5 orders (12 sample) + order_items -------------------------------------
Insert these 12 orders with their exact order_no, customer, city, date, status,
payment, channel and total. Then GENERATE order_items for each so that: the item
lines reference real products, the qty sums to "items", and the line totals sum
to "total". (The prototype stored only a count + total; you synthesise coherent
lines. A simple approach: pick products until the running total is closest to
the target, adjusting the last line's qty/unit.) Map customer names to the
seeded profiles where they exist (c1..c8); the four names not in the customer
list are guest orders (user_id null, keep customer_name).

order_no  | customer        | city       | date        | items | total | status     | pay         | channel
NX-24817  | Aarav Sharma    | Mumbai     | 2026-08-28  | 3     | 1497  | processing | UPI         | Web
NX-24816  | Diya Nair       | Kochi      | 2026-08-28  | 1     | 1299  | pending    | COD         | Web
NX-24815  | Kabir Mehta     | Ahmedabad  | 2026-08-27  | 5     | 2646  | shipped    | Card        | App
NX-24814  | Ananya Rao      | Bengaluru  | 2026-08-27  | 2     | 948   | delivered  | UPI         | Web
NX-24813  | Vivaan Gupta    | Delhi      | 2026-08-27  | 4     | 2196  | delivered  | UPI         | App
NX-24812  | Isha Kulkarni   | Pune       | 2026-08-26  | 1     | 649   | shipped    | Card        | Web
NX-24811  | Rohan Iyer      | Chennai    | 2026-08-26  | 6     | 3294  | processing | Net Banking | Web
NX-24810  | Meera Joshi     | Jaipur     | 2026-08-25  | 2     | 1098  | delivered  | UPI         | App
NX-24809  | Aditya Verma    | Lucknow    | 2026-08-25  | 3     | 1647  | cancelled  | COD         | Web
NX-24808  | Saanvi Reddy    | Hyderabad  | 2026-08-24  | 1     | 449   | delivered  | UPI         | Web
NX-24807  | Arjun Malhotra  | Chandigarh | 2026-08-24  | 4     | 2246  | shipped    | Card        | App
NX-24806  | Tara Menezes    | Goa        | 2026-08-23  | 2     | 1198  | delivered  | UPI         | Web
(These 12 use fixed order_no values BELOW the sequence start 24818, so live
checkouts won't collide.)

OPTIONAL (recommended for a realistic dashboard): also generate a larger
historical order set (roughly 1,200-1,300 orders spread across the last 12
months, referencing real products/customers) so the live analytics views land
near the prototype's headline figures: revenue ≈ ₹8.42L, orders ≈ 1,284, new
customers this month ≈ 612, AOV ≈ ₹656, and an upward monthly trend
(Jan→Dec: 74,82,96,88,104,118,132,146,128,158,184,212). If you skip this, the
dashboard will still work but show small numbers from the 12 sample orders.

--- 4.6 reviews (6) ----------------------------------------------------------
product | author        | rating | date       | status    | body
p09 | Meera Joshi    | 5 | 2026-08-27 | published | The Medjools arrived soft and fresh — genuinely the best I've had outside of Dubai.
p03 | Rohan Iyer     | 4 | 2026-08-26 | pending   | Great roast and not too salty. Would love a larger 500g pack option.
p13 | Ananya Rao     | 5 | 2026-08-26 | published | A pinch coloured my entire kheer a deep gold. Real Kashmiri saffron, you can smell it.
p01 | Vivaan Gupta   | 5 | 2026-08-25 | pending   | Crunchy and sweet, packing was sealed well. Reordering already.
p05 | Kabir Mehta    | 4 | 2026-08-24 | published | Vivid green pistachios, buttery. Shells open easily. Slightly pricey but worth it.
p10 | Isha Kulkarni  | 2 | 2026-08-23 | pending   | Tasty but arrived a day late and the pack was under-filled. Please check.

--- 4.7 testimonials (1) -----------------------------------------------------
quote: "I've been gifting Nuvana hampers to clients for two Diwalis now. The
packaging alone makes people think I spent twice as much."
author: "Kabir Mehta — Platinum member, Ahmedabad"   | is_featured true


================================================================================
SECTION 5 — BACKEND (server logic on Vercel)
================================================================================
Use Supabase from the server (service-role for admin/privileged paths; anon +
user JWT for user-scoped reads via RLS). Prefer Server Actions / Route Handlers.

Endpoints / actions to implement:
  Public / customer
   - GET catalog (filter by category, sort, search) — reads products (+category).
   - GET product detail (by id) incl. published reviews.
   - Cart: add / update qty / remove (server cart via carts/cart_items, or a
     signed cookie cart; either is fine).
   - POST checkout -> call place_order() RPC in a transaction; return order_no.
     Enforce the free-shipping threshold + flat shipping from store_settings.
   - Auth: sign up, log in, log out, session (Supabase Auth). On signup the
     profile row is auto-created by the trigger.
   - POST review (must be logged in) -> inserts as status='pending'.
   - GET my orders / my account (RLS shows only the caller's rows).
  Admin (gated by is_admin(); block non-admins at the server AND via RLS)
   - Products CRUD (+ image upload to the product-images bucket -> image_url).
   - Orders list + update status (pending->processing->shipped->delivered,
     or cancelled).
   - Reviews moderation: publish / hide / delete (drives the rating rollup).
   - Customers list (profiles + aggregated orders/spend).
   - Categories CRUD; testimonials CRUD; store_settings update.
   - Dashboard: read v_kpis, v_new_customers, v_orders_by_month,
     v_category_split, v_top_products.

Security must-haves:
   - Never expose SUPABASE_SERVICE_ROLE_KEY to the client bundle.
   - Recompute cart money server-side at checkout (place_order already does).
   - Verify is_admin() on the server for every admin action, not just in the UI.


================================================================================
SECTION 6 — FRONTEND (port the prototype, wire to live data)
================================================================================
Port the existing screens 1:1 in look, replacing window.NUVANA mock data with
live Supabase queries. Source components live in assets/storefront.js and
assets/admin.js; shared pieces in assets/ui.js; charts in assets/charts.js;
icons/illustrations in assets/icons.js; festive motifs in assets/graphics.js;
theme in assets/styles.css.

Storefront (customer)
   - Home: hero, category strip, bestsellers, new arrivals, the festive gift
     feature band, trust strip, testimonial, newsletter.
   - Shop: category filter + sort + search over live products.
   - Product page/modal: gallery (photo slot over illustration), unit selector,
     qty, add to cart, published reviews + "write a review" (logged-in).
   - Cart drawer + checkout: address, payment method, free-shipping threshold
     (₹999), place order -> confirmation with order_no.
   - Auth pages: sign up / log in / log out; "My account" + "My orders".
Admin (/admin, admin role only)
   - Dashboard (KPI cards + sparklines, revenue trend now-vs-prev, orders by
     month, category donut, top products, recent orders) from the views.
   - Orders table w/ slide-over detail + status changes.
   - Products grid/list w/ stock indicators + create/edit (incl. image upload).
   - Customers, Categories, Reviews moderation, Settings.
Keep hash-free real routes (e.g. /shop, /admin/orders) if you use Next.
Preserve responsive behaviour and the ₹ formatting.


================================================================================
SECTION 7 — DEPLOY (Vercel via MCP)
================================================================================
1. Commit the app to a git repo; create/link the Vercel project (Vercel MCP).
2. Add the env vars from Section 1 to the Vercel project (Production + Preview).
3. Ensure Supabase migrations + seed run against the linked project (apply via
   Supabase MCP; keep migration files in the repo, e.g. /supabase/migrations).
4. Deploy. Confirm the build succeeds and the production URL loads.
5. Set NEXT_PUBLIC_SITE_URL to the production URL and add it to Supabase Auth
   "Redirect URLs" / "Site URL" so email links and OAuth redirects work.
6. Return the production URL, Supabase project ref, and the admin login.


================================================================================
SECTION 8 — VERIFICATION CHECKLIST  (the owner will run these)
================================================================================
Fill in PASS/FAIL for each. Fix anything that fails before handing back.

Data & schema
 [ ] All tables exist; 6 categories, 14 products, 8 customer profiles + 1 admin,
     12 sample orders (each with order_items), 6 reviews, 1 testimonial,
     store_settings(1) all present.
 [ ] Product totals/units on each sample order's items sum to its total_inr.
 [ ] p10 shows out of stock; p06 shows low stock.
Auth & security
 [ ] Sign up a NEW customer -> a profiles row is auto-created (role customer).
 [ ] Log in as that customer; you can see ONLY your own orders (try to read
     another user's order id -> denied by RLS).
 [ ] Log in as admin@nuvana.shop -> /admin loads; a normal customer hitting
     /admin is refused (server-side, not just hidden in UI).
 [ ] Service-role key is NOT present anywhere in the browser bundle.
Shopping flow (the core test)
 [ ] Browse shop; category filter, sort and search work off live data.
 [ ] Open a product; add 2-3 items (mixed) to cart; totals + free-shipping at
     ₹999 compute correctly.
 [ ] Checkout -> you get an order_no like NX-24818 (from the sequence).
 [ ] That order now appears in the customer's "My orders" AND in admin Orders.
 [ ] The purchased products' stock DECREASED by the quantities bought.
Reviews
 [ ] Submit a review while logged in -> it is 'pending' and NOT visible on the
     storefront yet.
 [ ] In admin, publish it -> it appears on the product, and that product's
     rating_avg / reviews_count update automatically.
Admin & dashboard
 [ ] Edit a product (price/stock) and upload a photo -> the photo shows on the
     storefront over the illustration; clearing it falls back to the drawing.
 [ ] Change an order's status (e.g. processing -> shipped) and it persists.
 [ ] Dashboard KPIs, revenue trend, orders-by-month, category split and top
     products all render from live data (numbers change after a new checkout).
Deployment
 [ ] Production Vercel URL loads the storefront and admin over HTTPS.
 [ ] A hard refresh on a deep link (e.g. /shop) works (no 404).

DEFINITION OF DONE
 - Live URL works; a real signup -> browse -> checkout -> admin-sees-order ->
   stock-drops loop passes end to end; admin can manage catalog/orders/reviews;
   dashboard reflects live data; RLS keeps customers out of each other's data
   and out of admin. Hand over URL + Supabase ref + admin login + this
   checklist filled in.
================================================================================
END OF BRIEF
================================================================================
